export interface SupplierRequest {
  name: string;
  phoneNumber: string;
  address: string;
  matricule: string;
}

export interface SupplierResponse {
  id: number;
  name: string;
  phoneNumber: string;
  address: string;
  matricule: string;
}
